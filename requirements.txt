# Core runtime deps. The normalize/ module is pure stdlib (no deps) on purpose,
# so it can run anywhere — including a bare Colab cell — without installs.

# HTTP + retries/rate-limit handling for connectors (Phase 0+)
httpx>=0.27
tenacity>=8.2

# Config / secrets loading
python-dotenv>=1.0

# Dev / test
pytest>=8.0
